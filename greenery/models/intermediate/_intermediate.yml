version: 2

models:
  - name: int_sessions_agg
    description: Information about order addresses. 
    columns:
      - name: session_id
        tests:
          - not_null
          - unique
      - name: user_id
        tests:
          - not_null
      - name: product_id
        tests:
          - not_null